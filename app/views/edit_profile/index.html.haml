%header
  = render 'shared/header'

%nav.bread
  %ul.bread__list
    - breadcrumb :editProfile
    = render "layouts/breadcrumbs"
  
.editProfileMain
  = render 'shared/side_bar'
  .mainContainer
    .profileTitle
      プロフィール
    .editProfileForm
      = form_for "#" do |f|
        .userProfile
          .iconAndName
            .userIcon
              = image_tag 'eto_nezumi_fukubukuro.png', class: 'editUserIcon'
            .userName
              = f.text_field :text, class: "editUserName"
        .editBody
          = f.text_area :text, placeholder: "例）こんにちは☆ ご覧いただきありがとうございます！かわいいものやきれいめオフィスカジュアル中心に出品しています。服のサイズは主に9号です。気持ちよくお取引できるよう心がけていますので、商品や発送方法などご質問がありましたらお気軽にどうぞ♪", class: "editBodyTextarea"
          = f.submit "変更する", class: "editButton"

%footer
  = render 'shared/footer'
= render 'shared/exhibition-btn'