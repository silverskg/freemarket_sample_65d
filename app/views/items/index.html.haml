%script{:src => "https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"}
%script{:src => "https://cdn.jsdelivr.net/bxslider/4.2.12/jquery.bxslider.min.js"}


%header
  = render 'shared/header'
      
%body
  .slider
    = image_tag 'toppage/free.image-1.jpg', height: '390px', width: '100%', class: 'top-photo'
    = image_tag 'amazonIMGL0450_TP_V.jpg', height: '390px', width: '100%', class: 'top-photo'
  
  -# .images
  -#   = image_tag 'toppage/free.image-1.jpg', height: '390px', width: '100%', class: 'top-photo'
    -# %img(src="スクリーンショット 2019-12-19 19.03.35.png" class="top-photo")/
  .bar
    %div 人気のカテゴリー
    .bar__btn
      %a(href="#sec1" class="btn-gradient-radius")レディース
      %a(href="#sec2" class="btn-gradient-radius")メンズ
      %a(href="#sec3" class="btn-gradient-radius")家電・スマホ・カメラ
      %a(href="#sec4" class="btn-gradient-radius")おもちゃ・ホビー・グッズ
      -# レディースカテゴリー
.category
  .category__box
    .women
      .women__box
        .category-name{id:'sec1'}
          %p レディース新着アイテム
          .motto
            = link_to do
              もっと見る
              = icon('fas', 'angle-right')
              -# 上の段の写真
        .women__up
          %ul.item-boxes
            - @items.each do |item|
              - if item.category_id == 1
                = link_to item_path(item) do
                  %li.item-box
                    = image_tag item.images.first.image.to_s, height: '252px', width: '188px', class: 'box'
                    %p.price 
                      ¥
                      =item.price 
                    %p.name 
                      =item.name
      -# メンズカテゴリー
  .category__box
    .women
      .women__box
        .category-name{id:'sec2'}
          %p メンズ新着アイテム
          .motto
            = link_to do
              もっと見る
              = icon('fas', 'angle-right')
              -# 写真
        .women__up
          %ul.item-boxes
            - @items.each do |item|
              - if item.category_id == 2
                = link_to item_path(item) do
                  %li.item-box
                    = image_tag item.images.first.image.to_s, height: '252px', width: '188px', class: 'box'
                    %p.price 
                      ¥
                      =item.price 
                    %p.name 
                      =item.name
    -# カメラカテゴリー
  .category__box
    .women
      .women__box
        .category-name{id:'sec3'}
          %p 家電・スマホ・カメラ新着アイテム
          .motto
            = link_to do
              もっと見る
              = icon('fas', 'angle-right')
              -# 上の段の写真
        .women__up
          %ul.item-boxes
            - @items.each do |item|
              - if item.category_id == 8
                = link_to item_path(item) do
                  %li.item-box
                    = image_tag item.images.first.image.to_s, height: '252px', width: '188px', class: 'box'
                    %p.price 
                      ¥
                      =item.price 
                    %p.name 
                      =item.name
    -# おもちゃカテゴリー
  .category__box
    .women
      .women__box
        .category-name{id:'sec4'}
          %p おもちゃ・ホビー・グッズ新着アイテム
          .motto
            = link_to do
              もっと見る
              = icon('fas', 'angle-right')
              -# 上の段の写真
        .women__up
          %ul.item-boxes
            - @items.each do |item|
              - if item.category_id == 6
                = link_to item_path(item) do
                  %li.item-box
                    = image_tag item.images.first.image.to_s, height: '252px', width: '188px', class: 'box'
                    %p.price 
                      ¥
                      =item.price 
                    %p.name 
                      =item.name
  
.bar
  %div 人気のカテゴリー
  .bar__btn
    %a(href="#sec5" class="btn-gradient-radius")シャネル
    %a(href="#sec6" class="btn-gradient-radius")ルイヴィトン
    %a(href="#sec7" class="btn-gradient-radius")シュプリーム
    %a(href="#sec8" class="btn-gradient-radius")ナイキ

  -# シャネルカテゴリー
.category
  .category__box
    .chanel
      .category-name{id:'sec5'}
        %p シャネル新着アイテム
        .motto
          = link_to do
            もっと見る
            = icon('fas', 'angle-right')
            -# 上の段の写真
      .chanel__up
        %ul.item-boxes
          = link_to do
            - @items.each do |item|
              - if item.brand_id == 1
                = link_to item_path(item) do
                  %li.item-box
                    = image_tag item.images.first.image.to_s, height: '252px', width: '188px', class: 'box'
                    %p.price 
                      ¥
                      =item.price 
                    %p.name 
                      =item.name
      -# ビトンカテゴリー
  .category__box
    .chanel
      .category-name{id:'sec6'}
        %p ルイヴィトン新着アイテム
        .motto
          = link_to do
            もっと見る
            = icon('fas', 'angle-right')
            -# 上の段の写真
      .chanel__up
        %ul.item-boxes
          - @items.each do |item|
            - if item.brand_id == 3
              = link_to item_path(item) do
                %li.item-box
                  = image_tag item.images.first.image.to_s, height: '252px', width: '188px', class: 'box'
                  %p.price 
                    ¥
                    =item.price 
                  %p.name 
                    =item.name
    -# シュプリームカテゴリー
  .category__box
    .chanel
      .category-name{id:'sec7'}
        %p シュプリーム新着アイテム
        .motto
          = link_to do
            もっと見る
            = icon('fas', 'angle-right')
            -# 上の段の写真
      .chanel__up
        %ul.item-boxes
          - @items.each do |item|
            - if item.brand_id == 4
              = link_to item_path(item) do
                %li.item-box
                  = image_tag item.images.first.image.to_s, height: '252px', width: '188px', class: 'box'
                  %p.price 
                    ¥
                    =item.price 
                  %p.name 
                    =item.name
    -# ナイキカテゴリー
  .category__box
    .chanel
      .category-name{id:'sec8'}
        %p ナイキ新着アイテム
        .motto
          = link_to do
            もっと見る
            = icon('fas', 'angle-right')
            -# 上の段の写真
      .chanel__up
        %ul.item-boxes
          - @items.each do |item|
            - if item.brand_id == 2
              = link_to item_path(item) do
                %li.item-box
                  = image_tag item.images.first.image.to_s, height: '252px', width: '188px', class: 'box'
                  %p.price 
                    ¥
                    =item.price 
                  %p.name 
                    =item.name
%footer
  = render 'shared/footer'
= render 'shared/exhibition-btn'